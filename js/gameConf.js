// file to store game configuration and building parameters

// size of the board (N x N squares)
let nbSquaresSide = 8;
// set the zoom camera for both of the players
// the lower the value, the closer the camera is

let zoomRange = [30, 50, 60, 120, 200];
let zoomCameras = 30;
let gamma = 0.6;

const blockPlayerVisibility = 0.3;